# Configuration for /etc/init.d/octopipanel

# The init.d script will only run if this variable non-empty.
OCTOPIPANEL_USER=root

# Path to the OctoPrint executable, use this to override the default setting "/home/pi/OctoPiPanel/OctoPiPanel.py"
#DAEMON=/home/pi/OctoPiPanel/OctoPiPanel.py

# Should we run at startup?
START=yes

# Umask of files octoprint generates, Change this to 000 if running octoprint as its own, separate user
UMASK=022
